# Fahrzeugreservierung: Admin bucht stellvertretend — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Let a Feuerwehr-Admin create a vehicle booking on behalf of any member of their own
Heimatfeuerwehr, skipping the normal approval workflow, with the calendar showing the selected
member (not the admin) as the driver.

**Architecture:** `VehicleBooking.userId` keeps its existing meaning ("the driver") for the common
self-booking case; a new nullable `bookedByAdminId` field records which admin created a booking on
someone else's behalf (mirrors `DroneFlight.registeredBy`/`pilotUser`). `createVehicleBooking` gains
a second branch, gated by server-side re-validation of `canManageHeimatfeuerwehrFor`, that bypasses
the approval workflow entirely and sends two new informational emails instead of the existing
approval-request email.

**Tech Stack:** Next.js Server Actions, Prisma (additive migration), react-hook-form, Mailjet via
the existing `sendEmail()` wrapper.

## Global Constraints

- The existing self-booking path (`bookingForUserId` absent or equal to the acting user's own id)
  must be byte-for-byte behaviorally unchanged — this is the single most important regression-safety
  requirement, since most bookings will continue to be self-service.
- Never trust a client-submitted `bookingForUserId` without re-validating
  `canManageHeimatfeuerwehrFor` and same-organization membership server-side.
- All new email sends are best-effort and must never block the booking itself (try/catch, logged,
  never throws — same rule as every other email send in this module).
- No new permission functions — reuse `canManageHeimatfeuerwehrFor` exactly as it exists today.
- No automated test suite in this repo; verification is `npx tsc --noEmit`, `npm run build`, and
  manual verification against the local dev Postgres database.

---

### Task 1: Schema migration — `VehicleBooking.bookedByAdminId`

**Files:**
- Modify: `prisma/schema.prisma`
- Create: a new migration folder under `prisma/migrations/` (generated by the command below)

**Interfaces:**
- Produces: `VehicleBooking.bookedByAdminId: string | null` and its `bookedByAdmin` relation —
  Task 2 sets this field when creating a booking on behalf of someone else.

- [ ] **Step 1: Edit the `VehicleBooking` model in `prisma/schema.prisma`**

Find the model (currently ends with `@@index([vehicleId, startsAt])`) and change the `user` relation
line plus add the new field, exactly as follows:

```diff
    rejectionReason String?

+   // Gesetzt, wenn ein Feuerwehr-Admin diese Reservierung stellvertretend für ein anderes
+   // Mitglied angelegt hat (userId ist dann der Fahrer, nicht der Admin) - null bei einer
+   // normalen Selbst-Buchung. Analog zu DroneFlight.registeredBy/pilotUser. onDelete: SetNull,
+   // damit ein späteres Löschen des Admin-Kontos die Buchung selbst nicht mitreißt.
+   bookedByAdmin   User?   @relation("VehicleBookingBookedByAdmin", fields: [bookedByAdminId], references: [id], onDelete: SetNull)
+   bookedByAdminId String?
+
    vehicle Vehicle @relation(fields: [vehicleId], references: [id], onDelete: Cascade)
-   user    User    @relation(fields: [userId], references: [id], onDelete: Cascade)
+   user    User    @relation("VehicleBookingDriver", fields: [userId], references: [id], onDelete: Cascade)
    event   Event?

    @@index([vehicleId, startsAt])
  }
```

Naming both relations is required as soon as a model has two relations to the same target
(`User`) — Prisma cannot otherwise tell them apart.

- [ ] **Step 2: Update `User`'s back-relation for the now-named `VehicleBookingDriver` relation, and add the new one**

Find this line (currently un-named, in the `User` model's relation block):

```prisma
  vehicleBookings        VehicleBooking[]
```

Replace it and add the new back-relation right after:

```diff
-  vehicleBookings        VehicleBooking[]
+  vehicleBookings        VehicleBooking[]         @relation("VehicleBookingDriver")
+  vehicleBookingsAsAdmin VehicleBooking[]         @relation("VehicleBookingBookedByAdmin")
```

The field name `vehicleBookings` itself is unchanged, so no other code reading `user.vehicleBookings`
needs to change — only the relation now has an explicit name to match the model above.

- [ ] **Step 3: Generate and apply the migration**

```bash
npx prisma migrate dev --name vehicle_booking_admin_on_behalf
```

Expected: a new folder under `prisma/migrations/` containing an `ALTER TABLE "VehicleBooking" ADD
COLUMN "bookedByAdminId" TEXT` plus a new foreign key constraint — no data backfill needed (existing
rows get `NULL`, which is exactly what "not booked on behalf of anyone" means for them).

- [ ] **Step 4: Regenerate the Prisma client and verify**

```bash
npx prisma generate
npx tsc --noEmit
```

Expected: both succeed with no errors (the schema change alone shouldn't affect any existing
TypeScript — nothing yet reads or writes `bookedByAdminId`).

- [ ] **Step 5: Commit**

```bash
git add prisma/schema.prisma prisma/migrations
git commit -m "feat: add VehicleBooking.bookedByAdminId for admin-on-behalf-of bookings"
```

---

### Task 2: Server-side logic — `createVehicleBooking` admin branch + two new emails

**Files:**
- Modify: `src/app/(app)/meine-feuerwehr/actions.ts`
- Modify: `src/lib/heimatfeuerwehr/notify-vehicle-booking.ts`

**Interfaces:**
- Consumes: `VehicleBooking.bookedByAdminId` (Task 1), `canManageHeimatfeuerwehrFor(user, organizationId): boolean` (existing, `@/lib/auth/permissions`).
- Produces: `createVehicleBooking` now reads an optional `bookingForUserId` field from the submitted
  `FormData` (Task 3 is what actually sends it from the UI — this task works correctly even before
  Task 3 exists, since a `FormData` without that key behaves exactly like today).
- Produces: two new exported functions in `notify-vehicle-booking.ts` —
  `sendVehicleBookingAdminInfoEmail(ctx: AdminBookingEmailContext, toEmails: string[]): Promise<void>`
  and `sendVehicleBookingDriverNotificationEmail(ctx: AdminBookingEmailContext): Promise<void>`, both
  never throwing (self-contained try/catch, matching `sendVehicleBookingApprovalRequest`'s existing
  pattern in this same file).

- [ ] **Step 1: Add the two new email functions to `notify-vehicle-booking.ts`**

Add this new interface and these two functions at the end of the file (after
`sendVehicleBookingDecisionEmail`):

```ts
interface AdminBookingEmailContext {
  startsAt: Date;
  endsAt: Date;
  details: string;
  vehicleTaktischeBezeichnung: string;
  vehicleKennzeichen: string;
  organizationLabel: string;
  adminName: string;
  driverName: string;
  driverEmail: string;
}

/**
 * Rein informative E-Mail an die für die Feuerwehr hinterlegten fahrzeugReservierungEmails, wenn
 * ein Admin stellvertretend für ein anderes Mitglied gebucht hat - keine Genehmigen/Ablehnen-Links,
 * da hier nichts zu entscheiden ist (die Reservierung ist bereits GENEHMIGT). Eine E-Mail PRO
 * Empfänger, nie ein gemeinsames To/Cc - dieselbe Regel wie sendVehicleBookingApprovalRequest. Jeder
 * Versand einzeln try/catch-abgesichert, die Funktion selbst wirft nie. Kein Versand, falls
 * toEmails leer ist (gleiches Verhalten wie beim bestehenden Genehmigungs-Anfrage-Pfad).
 */
export async function sendVehicleBookingAdminInfoEmail(ctx: AdminBookingEmailContext, toEmails: string[]): Promise<void> {
  if (toEmails.length === 0) return;

  const range = formatRange(ctx.startsAt, ctx.endsAt);
  const subject = `Fahrzeug reserviert: ${ctx.vehicleTaktischeBezeichnung} (${ctx.organizationLabel})`;
  const textPart = [
    `${ctx.adminName} hat eine Fahrzeug-Reservierung für ${ctx.driverName} angelegt (bereits genehmigt):`,
    '',
    `Fahrzeug: ${ctx.vehicleTaktischeBezeichnung} (${ctx.vehicleKennzeichen})`,
    `Zeitraum: ${range}`,
    ctx.details ? `Details: ${ctx.details}` : null,
  ]
    .filter((line) => line !== null)
    .join('\n');
  const htmlPart = [
    `<p>${escapeHtml(ctx.adminName)} hat eine Fahrzeug-Reservierung für ${escapeHtml(ctx.driverName)} angelegt (bereits genehmigt):</p>`,
    '<ul>',
    `<li>Fahrzeug: ${escapeHtml(ctx.vehicleTaktischeBezeichnung)} (${escapeHtml(ctx.vehicleKennzeichen)})</li>`,
    `<li>Zeitraum: ${escapeHtml(range)}</li>`,
    ctx.details ? `<li>Details: ${escapeHtml(ctx.details)}</li>` : '',
    '</ul>',
  ].join('');

  for (const to of toEmails) {
    try {
      await sendEmail({ to, subject, textPart, htmlPart });
    } catch (error) {
      console.error('Info-E-Mail für stellvertretende Fahrzeug-Reservierung fehlgeschlagen:', error);
    }
  }
}

/**
 * Benachrichtigt das Mitglied, für das ein Admin stellvertretend gebucht hat - verhindert, dass
 * jemand erst im Kalender entdeckt, dass für ihn ein Fahrzeug reserviert wurde. Best-effort wie
 * jeder andere E-Mail-Versand in diesem Modul, wirft nie.
 */
export async function sendVehicleBookingDriverNotificationEmail(ctx: AdminBookingEmailContext): Promise<void> {
  const range = formatRange(ctx.startsAt, ctx.endsAt);

  try {
    await sendEmail({
      to: ctx.driverEmail,
      toName: ctx.driverName,
      subject: `Für dich wurde ein Fahrzeug reserviert: ${ctx.vehicleTaktischeBezeichnung}`,
      textPart: [
        `${ctx.adminName} hat für dich eine Fahrzeug-Reservierung angelegt.`,
        '',
        `Fahrzeug: ${ctx.vehicleTaktischeBezeichnung} (${ctx.vehicleKennzeichen})`,
        `Zeitraum: ${range}`,
        ctx.details ? `Details: ${ctx.details}` : null,
        '',
        'Die Reservierung ist bereits genehmigt und im Kalender deiner Feuerwehr sichtbar.',
      ]
        .filter((line) => line !== null)
        .join('\n'),
      htmlPart: [
        `<p>${escapeHtml(ctx.adminName)} hat für dich eine Fahrzeug-Reservierung angelegt.</p>`,
        '<ul>',
        `<li>Fahrzeug: ${escapeHtml(ctx.vehicleTaktischeBezeichnung)} (${escapeHtml(ctx.vehicleKennzeichen)})</li>`,
        `<li>Zeitraum: ${escapeHtml(range)}</li>`,
        ctx.details ? `<li>Details: ${escapeHtml(ctx.details)}</li>` : '',
        '</ul>',
        '<p>Die Reservierung ist bereits genehmigt und im Kalender deiner Feuerwehr sichtbar.</p>',
      ].join(''),
    });
  } catch (error) {
    console.error('Benachrichtigungs-E-Mail an Fahrer für stellvertretende Fahrzeug-Reservierung fehlgeschlagen:', error);
  }
}
```

- [ ] **Step 2: Update imports in `src/app/(app)/meine-feuerwehr/actions.ts`**

```diff
- import { assertPermission, canManageVehicleBooking } from '@/lib/auth/permissions';
+ import { assertPermission, canManageHeimatfeuerwehrFor, canManageVehicleBooking } from '@/lib/auth/permissions';
  import { vehicleBookingSchema, parseVehicleBookingFormData } from '@/lib/validation/vehicle-booking.schema';
  import { findOverlappingBooking } from '@/lib/heimatfeuerwehr/vehicle-availability';
- import { sendVehicleBookingApprovalRequest } from '@/lib/heimatfeuerwehr/notify-vehicle-booking';
+ import {
+   sendVehicleBookingApprovalRequest,
+   sendVehicleBookingAdminInfoEmail,
+   sendVehicleBookingDriverNotificationEmail,
+ } from '@/lib/heimatfeuerwehr/notify-vehicle-booking';
```

- [ ] **Step 3: Insert the new admin-on-behalf-of branch into `createVehicleBooking`**

Insert this new block **immediately after** the existing overlap check (right after the `if
(overlap) { return { error: ... }; }` block) and **before** the existing `const approvalEmails = ...`
line — i.e. the new branch runs first and, when it applies, returns/redirects before the old
approval-vs-immediate logic ever runs:

```diff
    const overlap = await findOverlappingBooking(data.vehicleId, startsAt, endsAt);
    if (overlap) {
      return {
        error: `Das Fahrzeug ist in diesem Zeitraum bereits von ${overlap.user.firstName} ${overlap.user.lastName} reserviert.`,
      };
    }

+   // Stellvertretende Buchung: nur wenn ein anderer Wert als die eigene ID übermittelt wurde UND
+   // der handelnde Nutzer tatsächlich Admin dieser Feuerwehr ist - serverseitig erneut geprüft,
+   // nie nur der Formular-UI vertraut. Siehe docs/superpowers/specs/2026-08-28-
+   // fahrzeugreservierung-admin-stellvertretend-design.md.
+   const bookingForUserIdRaw = formData.get('bookingForUserId');
+   const bookingForUserId =
+     typeof bookingForUserIdRaw === 'string' && bookingForUserIdRaw.length > 0 ? bookingForUserIdRaw : null;
+   const isOnBehalfOf = bookingForUserId !== null && bookingForUserId !== user.id;
+
+   if (isOnBehalfOf) {
+     if (!canManageHeimatfeuerwehrFor(user, vehicle.organizationId)) {
+       return { error: 'Keine Berechtigung, für ein anderes Mitglied zu reservieren.' };
+     }
+
+     const driver = await prisma.user.findUnique({
+       where: { id: bookingForUserId! },
+       select: { id: true, firstName: true, lastName: true, email: true, homeOrganizationId: true },
+     });
+     if (!driver || driver.homeOrganizationId !== vehicle.organizationId) {
+       return { error: 'Das ausgewählte Mitglied gehört nicht zu dieser Feuerwehr.' };
+     }
+
+     const driverName = `${driver.firstName} ${driver.lastName}`;
+     const organizationLabel = vehicle.organization.shortName ?? vehicle.organization.name;
+
+     const booking = await prisma.vehicleBooking.create({
+       data: {
+         vehicleId: data.vehicleId,
+         userId: driver.id,
+         bookedByAdminId: user.id,
+         startsAt,
+         endsAt,
+         details: data.details,
+         status: 'GENEHMIGT',
+       },
+     });
+
+     const bookingEvent = await prisma.event.create({
+       data: {
+         title: `Fahrzeug: ${vehicle.taktischeBezeichnung} (${driverName})`,
+         startsAt,
+         endsAt,
+         organizationId: vehicle.organizationId,
+         isSectionWide: false,
+         category: 'ALLGEMEIN',
+         createdById: user.id,
+         vehicleBookingId: booking.id,
+       },
+     });
+     await pushEventToGoogleCalendar(bookingEvent);
+
+     const emailCtx = {
+       startsAt,
+       endsAt,
+       details: data.details,
+       vehicleTaktischeBezeichnung: vehicle.taktischeBezeichnung,
+       vehicleKennzeichen: vehicle.kennzeichen,
+       organizationLabel,
+       adminName: user.name,
+       driverName,
+       driverEmail: driver.email,
+     };
+     await sendVehicleBookingAdminInfoEmail(emailCtx, vehicle.organization.fahrzeugReservierungEmails);
+     await sendVehicleBookingDriverNotificationEmail(emailCtx);
+
+     revalidatePath('/meine-feuerwehr');
+     revalidatePath('/kalender');
+     redirect('/meine-feuerwehr');
+   }
+
    const approvalEmails = vehicle.organization.fahrzeugReservierungEmails;
    const needsApproval = approvalEmails.length > 0;
```

`redirect()` throws internally (a Next.js control-flow signal), so nothing after this new block runs
when `isOnBehalfOf` is true — the existing self-booking code below it is reached only when the
branch condition is false, which is exactly the "field absent or equal to own id" case the Global
Constraints require to stay unchanged.

- [ ] **Step 4: Verify**

```bash
npx tsc --noEmit
npm run build
```

Expected: both succeed. This task cannot be fully exercised yet since Task 3's form doesn't send
`bookingForUserId` — verify instead with a standalone script or `curl`/direct Server Action call
against the local dev DB: call `createVehicleBooking` with a `FormData` that includes a valid
`bookingForUserId` for a different, real member of the same organization (using an account that
passes `canManageHeimatfeuerwehrFor`), then confirm via `psql`:
```sql
SELECT "userId", "bookedByAdminId", status FROM "VehicleBooking" ORDER BY "createdAt" DESC LIMIT 1;
```
Expected: `status = 'GENEHMIGT'`, `userId` = the target member's id, `bookedByAdminId` = the acting
admin's id. Also confirm a linked `Event` row exists with a title containing the driver's name, not
the admin's. Then repeat the existing self-booking flow (no `bookingForUserId`, or an admin booking
for themselves) and confirm it behaves exactly as before (regression check).

- [ ] **Step 5: Commit**

```bash
git add "src/app/(app)/meine-feuerwehr/actions.ts" src/lib/heimatfeuerwehr/notify-vehicle-booking.ts
git commit -m "feat: let admins create vehicle bookings on behalf of other members"
```

---

### Task 3: Form UI — "Fahrzeugreservierung für" selector

**Files:**
- Modify: `src/app/(app)/meine-feuerwehr/buchen/page.tsx`
- Modify: `src/app/(app)/meine-feuerwehr/buchen/booking-form.tsx`

**Interfaces:**
- Consumes: `canManageHeimatfeuerwehrFor` (existing), `NOT_DEACTIVATED_WHERE` (existing,
  `@/lib/auth/user-status`), the `bookingForUserId` FormData field Task 2's action already reads.
- Produces: `BookingForm` gains three new optional props — `bookingForMembers?: { id: string;
  firstName: string; lastName: string }[]`, `currentUserId?: string`, `currentUserName?: string` —
  all three either all present (admin) or all absent (non-admin); when absent, the form renders
  identically to today.

- [ ] **Step 1: Load admin status and member list in `buchen/page.tsx`**

Replace the whole file:

```tsx
import { requireUser } from '@/lib/auth/session';
import { prisma } from '@/lib/db/prisma';
import { canManageHeimatfeuerwehrFor } from '@/lib/auth/permissions';
import { NOT_DEACTIVATED_WHERE } from '@/lib/auth/user-status';
import { createVehicleBooking } from '../actions';
import { BookingForm } from './booking-form';

export default async function FahrzeugBuchenPage({
  searchParams,
}: {
  searchParams: Promise<{ vehicleId?: string }>;
}) {
  const user = await requireUser();
  const { vehicleId } = await searchParams;

  const vehicles = await prisma.vehicle.findMany({
    where: { organizationId: user.homeOrganizationId, isActive: true },
    orderBy: { taktischeBezeichnung: 'asc' },
    select: { id: true, taktischeBezeichnung: true, kennzeichen: true },
  });

  const initialVehicleId = vehicleId && vehicles.some((v) => v.id === vehicleId) ? vehicleId : undefined;

  // "Fahrzeugreservierung für"-Auswahl nur für Admins der eigenen Heimatfeuerwehr - siehe
  // docs/superpowers/specs/2026-08-28-fahrzeugreservierung-admin-stellvertretend-design.md.
  const isAdmin = canManageHeimatfeuerwehrFor(user, user.homeOrganizationId);
  const bookingForMembers = isAdmin
    ? await prisma.user.findMany({
        where: { homeOrganizationId: user.homeOrganizationId, ...NOT_DEACTIVATED_WHERE },
        orderBy: [{ lastName: 'asc' }, { firstName: 'asc' }],
        select: { id: true, firstName: true, lastName: true },
      })
    : undefined;

  return (
    <div className="flex flex-col gap-4">
      <h1 className="text-lg font-semibold text-neutral-900">Fahrzeug Reservierungen</h1>
      {vehicles.length === 0 ? (
        <p className="text-sm text-neutral-500">Für deine Feuerwehr sind noch keine Fahrzeuge hinterlegt.</p>
      ) : (
        <BookingForm
          vehicles={vehicles}
          action={createVehicleBooking}
          initialVehicleId={initialVehicleId}
          bookingForMembers={bookingForMembers}
          currentUserId={isAdmin ? user.id : undefined}
          currentUserName={isAdmin ? user.name : undefined}
        />
      )}
    </div>
  );
}
```

- [ ] **Step 2: Add the new field to `booking-form.tsx`**

Update the props interface and `useForm` defaults:

```diff
+ interface MemberOption {
+   id: string;
+   firstName: string;
+   lastName: string;
+ }
+
  interface BookingFormValues {
    vehicleId: string;
+   bookingForUserId: string;
    date: string;
    startTime: string;
    endTime: string;
    details: string;
  }

  interface BookingFormProps {
    vehicles: VehicleOption[];
    action: (prevState: VehicleBookingFormState, formData: FormData) => Promise<VehicleBookingFormState>;
    initialVehicleId?: string;
+   bookingForMembers?: MemberOption[];
+   currentUserId?: string;
+   currentUserName?: string;
  }

- export function BookingForm({ vehicles, action, initialVehicleId }: BookingFormProps) {
+ export function BookingForm({
+   vehicles,
+   action,
+   initialVehicleId,
+   bookingForMembers,
+   currentUserId,
+   currentUserName,
+ }: BookingFormProps) {
    const [pending, startTransition] = useTransition();
    const [serverError, setServerError] = useState<string | undefined>();

    const {
      register,
      control,
      handleSubmit,
      formState: { errors },
    } = useForm<BookingFormValues>({
-     defaultValues: { vehicleId: initialVehicleId ?? vehicles[0]?.id ?? '', date: '', startTime: '', endTime: '', details: '' },
+     defaultValues: {
+       vehicleId: initialVehicleId ?? vehicles[0]?.id ?? '',
+       bookingForUserId: currentUserId ?? '',
+       date: '',
+       startTime: '',
+       endTime: '',
+       details: '',
+     },
    });
```

Update `onSubmit` to include the new field only when the picker is actually shown (non-admins never
send this key at all, matching Task 2's "field absent" case exactly):

```diff
      const formData = new FormData();
      formData.set('vehicleId', values.vehicleId);
+     if (bookingForMembers) formData.set('bookingForUserId', values.bookingForUserId);
      formData.set('startsAt', `${values.date}T${values.startTime}`);
      formData.set('endsAt', `${values.date}T${values.endTime}`);
      formData.set('details', values.details);
```

Add the new `<select>` right after the existing "Fahrzeug" field block, before "Datum":

```diff
        {errors.vehicleId && <p className="text-sm text-red-700">{errors.vehicleId.message}</p>}
      </div>

+     {bookingForMembers && (
+       <div className="flex flex-col gap-1">
+         <label className="text-sm font-medium text-neutral-700">Fahrzeugreservierung für</label>
+         <select {...register('bookingForUserId')} className="rounded border border-neutral-300 px-3 py-2">
+           {currentUserId && (
+             <option value={currentUserId}>Ich selbst{currentUserName ? ` (${currentUserName})` : ''}</option>
+           )}
+           {bookingForMembers
+             .filter((member) => member.id !== currentUserId)
+             .map((member) => (
+               <option key={member.id} value={member.id}>
+                 {member.firstName} {member.lastName}
+               </option>
+             ))}
+         </select>
+       </div>
+     )}
+
      <div className="flex flex-col gap-1">
        <label className="text-sm font-medium text-neutral-700">Datum</label>
```

- [ ] **Step 3: Verify**

```bash
npx tsc --noEmit
npm run build
```

Expected: both succeed. Then start the dev server and check live in the browser:
1. Log in as a plain (non-admin) member, open `/meine-feuerwehr/buchen` — confirm **no**
   "Fahrzeugreservierung für" field appears, and the form still submits exactly as before.
2. Log in as a Feuerwehr-Admin, open the same page — confirm the new field appears, defaulted to
   "Ich selbst (...)", followed by every other active member of that Feuerwehr (not including a
   duplicate entry for the admin themselves).
3. As the admin, submit a booking for another member — confirm redirect to `/meine-feuerwehr`, then
   check `/kalender` shows the new event with the selected member's name in the title, and
   `/admin/heimatfeuerwehr`'s Fahrzeug-Reservierungen table shows the booking as already `Genehmigt`.
4. As the admin, submit a booking choosing "Ich selbst" — confirm this behaves exactly like the
   pre-existing self-booking flow (approval workflow applies if `fahrzeugReservierungEmails` is
   configured for that Feuerwehr).

- [ ] **Step 4: Commit**

```bash
git add "src/app/(app)/meine-feuerwehr/buchen/page.tsx" "src/app/(app)/meine-feuerwehr/buchen/booking-form.tsx"
git commit -m "feat: add \"Fahrzeugreservierung für\" selector for admin bookings"
```
